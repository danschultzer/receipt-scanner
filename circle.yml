machine:
  node:
    version: 5.0.0

dependencies:
  cache_directories:
    - ~/opencv-3.1.0

  pre:
    - sudo apt-get update
    - sudo apt-get install poppler-utils
    - sudo apt-get install tesseract-ocr
    - sh bin/circle-install.sh
